{
  "name": "advent-of-code-2023",
  "author": "Max Davidson <davidsonmax@gmail.com>",
  "description": "Solutions to Advent of Code 2023",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": "^20.9.0",
    "npm": "^10.1.0"
  },
  "exports": null,
  "scripts": {
    "test": "NODE_ENV=test node --test --import=tsimp/import --enable-source-maps --watch $(git ls-files --others --exclude-standard | grep test.ts)",
    "test:all": "NODE_ENV=test node --test --import=tsimp/import --enable-source-maps $(find src -name '*.test.ts')"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "prettier": "~3.1.0",
    "typescript": "~5.3.0",
    "tsimp": "^2.0.5"
  }
}
